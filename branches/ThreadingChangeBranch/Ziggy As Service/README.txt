These files, when dropped into the main binary directory, will allow ziggy to run as a service on a windows machine.

Should work with Win XP, Vista, and 7 (These are 32bit, 64 bit versions exist, but 32 bit versions work on both 32 and 64 bit)